#include <stdio.h>
#define PAGELEN 2
#define LINELEN 512
void do_more(FILE *)
int see_more();

int main(int argc,char** argv)
{
	FILE* fp;
	if(ac== 1)
	{
		do_more(stdin);
	}
	else
	{
		while(--ac)
		{
			
